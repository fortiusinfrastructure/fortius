/**
 * Mock data layer — transitional source of truth for IEAM content.
 *
 * These files were copied verbatim from the original Vite IEAM app
 * (`ieam/src/data/*.ts`). They will be replaced by Supabase queries in Phase 3
 * once `team_members` table and content migration scripts are ready.
 */
export * from './articles';
export * from './events';
export * from './team';
